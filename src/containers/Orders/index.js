import React from 'react'
import Layout from '../../components/Layout'

/**
* @author
* @function Orders
**/

const Orders = (props) => {
    return (
        <Layout sidebar={true}>
            Orders
        </Layout>
    )
}

export default Orders;

